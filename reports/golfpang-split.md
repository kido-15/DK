# 골팡 지역별 분할 수집 — 검증

`claude/stoic-heisenberg-cfuysm` 의 `3ef581c`(하루치를 지역 5개로 나누어 받는다)가
실제로 누락을 줄이는지 확인한 기록이다. 코드는 새로 만들지 않았다.

- 실행 환경: 2026-09-18 (UTC), `www.golfpang.com` 만, 로그인하지 않음, 읽기만 함
- 시작 전 `python3 tests/test_full_collect.py` → **Ran 33 tests, OK (33건 통과)**

> 이 문서는 단계마다 이어 붙인다. ② 지역코드 확인은 수집 전에 끝나므로 먼저 적었다.

---

## ② 지역코드 매핑이 맞는가 — **맞습니다**

### 2-1. 화면의 `sct` 드롭다운 원문

`https://www.golfpang.com/web/round/booking_list.do` 를 받아 `<select id="sct">` 를
그대로 읽었다.

```html
<select name="sct" id="sct" onchange="Qcatelist_chk(this.value);">
    <option value="">전체</option>
    <option value="5" >한강이남</option>
    <option value="1" >강북/경춘</option>
    <option value="4" >충청</option>
    <option value="8" >원주/영동</option>
    <option value="16" >영호남/제주</option>
</select>
```

설정(`config/sources.golfpang.json` 의 `request.split.labels`)과 **글자까지 같다.**

| 코드 | 화면 option 라벨 | 설정에 적힌 라벨 | 일치 |
|---|---|---|---|
| `5` | 한강이남 | 한강이남 | ✅ |
| `1` | 강북/경춘 | 강북/경춘 | ✅ |
| `4` | 충청 | 충청 | ✅ |
| `8` | 원주/영동 | 원주/영동 | ✅ |
| `16` | 영호남/제주 | 영호남/제주 | ✅ |

값이 있는 option 은 이 **5개가 전부**다. 나머지는 `value=""`(전체) 하나뿐이다.

같은 화면의 날짜 띠도 같은 5개를 같은 순서로 쓴다. 띠 머리글이
`한강이남 / 강북/경춘 / 충청 / 원주/영동 / 영호남/제주` 이고, 각 칸의 onclick 이
`selectQuick('2026-09-19','5')` … `('2026-09-19','16')` 로 **같은 순서로 대응**한다.

### 2-2. 받은 행의 첫 칸(지역 이름)이 라벨과 맞는가 — **맞습니다**

응답 표의 첫 칸은 머리글이 `지역` 이다(`selectOrder('sector_desc')`).
각 sector 로 받은 행의 첫 칸을 세었다.

2026-10-13 (전량, 날짜 하나를 끝까지 받음):

| 요청 sector | 받은 행 | 첫 칸(지역) 분포 |
|---|---|---|
| `5` | 83 | 한강이남 83 |
| `1` | 58 | 강북/경춘 58 |
| `4` | 34 | 충청 34 |
| `8` | 99 | 원주/영동 99 |
| `16` | 2 | 호남 2 |

**한 행도 섞이지 않았다.** `16` 만 첫 칸이 라벨(`영호남/제주`)과 글자가 다른데,
이건 `16` 이 상위 묶음이기 때문이다. 화면의 세부지역 드롭다운이 그렇게 되어 있다.

```html
<select name="sct3" id="sct3">
    <option value="16" >전체</option>   ← 영호남/제주 전체
    <option value="3" >영남</option>
    <option value="6" >호남</option>
    <option value="7" >제주</option>
</select>
```

`16` 이 영남·호남·제주를 실제로 다 담는지 따로 확인했다.

| 날짜 | sector=16 총 | 첫 칸 분포 |
|---|---|---|
| 2026-09-19 | 96 | 호남 55, 영남 41 |
| 2026-09-20 | 68 | 호남 34, 영남 32, 제주 2 |

`3`/`6`/`7` 은 `16` 의 **하위**이므로 분할 목록에 따로 넣으면 안 된다(중복이 된다).
지금 설정이 맞다.

### 2-3. 5개 지역 합이 전국 전체와 맞는가 — **맞습니다 (가장 중요한 확인)**

응답 머리에 서버가 직접 센 건수가 적혀 있다는 것을 확인했다.

```html
<span class="num_product">총 <strong>34 </strong> 개 타임이 검색되었습니다..</span>
```

이 숫자가 실제 행 수와 같은지 먼저 확인했다. 2026-10-13 `sector=''` 를 빈 페이지가
나올 때까지 받으니 **총=276, 받은 행 276, 고유 row_id 276** 으로 정확히 같았다.
그래서 이 숫자를 기준값으로 쓴다.

**(가) row_id 집합 대조 — 2026-10-13 전량**

`sector=''`(전국) 전량과 5개 sector 전량을 각각 끝까지 받아 `row_id` 로 비교했다.

```
sector=''  총=276  받은행=276  고유=276
sector=5   총=83   sector=1  총=58   sector=4  총=34   sector=8  총=99   sector=16 총=2
                                                    5개 합 = 276

전체 고유 276 / 5개 합집합 고유 276
전체에만 있는 row_id (= 분할이 못 덮는 행)  : 0
분할에만 있는 row_id (= 훑는 사이 새로 들어온 행) : 0
두 개 이상 sector 에 동시에 나온 row_id       : 0
```

**빠진 지역코드는 없다.** 5개가 전국을 빈틈없이, 겹치지 않게 덮는다.

**(나) 총 건수 대조 — 실제 수집 대상 날짜**

비교 실행에 쓸 날짜에도 같은 확인을 했다(요청 1회씩, 2026-09-18 07:1x UTC).

| 날짜 | `5` | `1` | `4` | `8` | `16` | 5개 합 | `sector=''` 전체 총 | 차이 |
|---|---|---|---|---|---|---|---|---|
| 2026-09-18 | 492 | 336 | 485 | 222 | 10 | **1,545** | **1,545** | 0 |
| 2026-09-19 | 3,347 | 2,120 | 3,451 | 1,083 | 96 | **10,097** | **10,098** | +1 |
| 2026-09-20 | 3,219 | 2,395 | 3,287 | 1,093 | 68 | **10,062** | **10,062** | 0 |

09-19 의 1건 차이는 6번의 요청이 1.6초 간격으로 이어지는 동안 매물이 하나 드나든
것이다(0.01%). 구조적인 누락으로 보기 어렵다.

**결론: 지역 분할은 전국을 덮는다. 나누어서 손해 보는 행은 없다.**

### 2-4. 덤으로 나온 것 — 날짜 띠 숫자는 기준값으로 쓰면 안 된다

앞선 세션은 "사이트 표시 건수" 로 날짜 띠(`selectQuick` 칸)의 숫자를 썼다.
그런데 띠 숫자와 서버가 응답에 적어 주는 `총 N` 이 서로 다르다. 같은 시각에
둘 다 받아 비교했다.

| 날짜 | 날짜 띠 합계 | 응답의 `전체 총` | 띠가 더 많음 |
|---|---|---|---|
| 2026-09-18 | 1,784 | 1,545 | +239 (+15.5%) |
| 2026-09-19 | 10,134 | 10,098 | +36 (+0.4%) |
| 2026-09-20 | 10,326 | 10,062 | +264 (+2.6%) |
| 2026-10-13 | 480 | 276 | +204 (+73.9%) |

띠가 항상 더 크고, 지역별로 보면 차이가 `충청` 에 몰려 있다
(2026-10-13: 띠 241 vs 총 34). **왜 다른지는 확인하지 못했습니다.**
띠가 갱신이 늦은 캐시인지, 목록에 안 나오는 매물까지 세는지 알아내지 못했다.

다만 `총 N` 쪽은 실제 행 수와 정확히 일치하는 것을 확인했으므로(2-3 가),
아래 ① 비교표에서는 **`총 N` 을 기준값으로 쓰고 띠 숫자도 함께 적는다.**

앞선 세션의 "09-19 가 사이트 표시보다 3.7~6.3% 부족" 은 띠를 기준으로 한 숫자다.
기준값 자체가 흔들리는 값이었으므로 그 부족분이 전부 수집 누락이었다고 보기는
어렵다. 얼마가 기준값 문제이고 얼마가 누락인지는 아래 ① 에서 다시 잰다.

---

## ① 두 방식 비교

### 1-0. 기준값을 무엇으로 잡았는가

응답 머리의 `총 N 개 타임이 검색되었습니다` 를 쓴다(②-3 에서 실제 행 수와
일치하는 것을 확인했다). 매물이 실시간으로 드나들어 이 값이 계속 움직이므로,
**각 실행 직전과 직후에 한 번씩** 받아 두 개를 다 적는다.
날짜 띠 숫자는 ②-4 의 이유로 기준값으로 쓰지 않는다(참고로만 적는다).

### 1-1. 2026-09-18 (사양에 적힌 `--days 1`. 오늘 = 16페이지짜리 날)

```
python3 scripts/collect_full.py golfpang --days 1 --no-split --save reports/split-off.csv --no-snapshot
python3 scripts/collect_full.py golfpang --days 1            --save reports/split-on.csv  --no-snapshot
```

실행 간격: `--no-split` 종료 07:19:41 → `split` 시작 07:20:56, **75초**.

기준값(`sector=''` 전체 총):

| 시각(UTC) | 무엇 | 전체 총 |
|---|---|---|
| 07:17:41 | `--no-split` 직전 | 1,436 |
| 07:19:41 | `--no-split` 직후 | 1,427 |
| 07:20:28 | `split` 직전 | 1,422 |
| 07:23:0x | `split` 직후 | 1,408 |

오늘치는 티타임이 지나가면서 목록이 **분당 5건꼴로 줄어든다.** 그래서 직후
기준으로 재면 확보율이 100%를 넘을 수 있다.

| 항목 | `--no-split` (한 번에) | `split` (지역 5개) |
|---|---|---|
| 고유 `row_id` | **1,413** | **1,412** |
| 확보율 (직전 기준) | 1,413 / 1,436 = **98.4%** | 1,412 / 1,422 = **99.3%** |
| 확보율 (직후 기준) | 1,413 / 1,427 = **99.0%** | 1,412 / 1,408 = **100.3%** |
| 요청 횟수 | 16회 | 22회 |
| 소요 시간 | 88초 | 97초 |
| 걸러진 중복 | 20건 | **0건** |
| 받은 행(거르기 전) | 1,433 | 1,412 |

두 결과의 겹침:

```
교집합 1,389   --no-split 에만 24   split 에만 23   합집합 1,436
```

한쪽에만 있는 행이 **24 / 23 으로 거의 대칭**이다. 한 방식이 다른 방식이 본 것을
놓친 것이 아니라, 75초 사이에 매물이 드나든 것으로 읽힌다.

**이 날짜로는 분할의 효과를 판정할 수 없다.** 하루가 16페이지(약 1,400건)뿐이라
한 번에 받아도 88초면 끝나고, 애초에 목록이 흔들릴 틈이 거의 없다. 문제가
관측된 것은 102페이지 22분짜리 날이었다. 그래서 아래 1-2 를 따로 돌렸다.

### 1-2. 2026-09-19 (문제가 관측된 날. 약 102페이지)

(실행 중 — 끝나면 채운다)

## ③ 묶음 경계 중복

(수집 실행 후 채운다)

## 안 된 것 / 확인 못 한 것

(마지막에 채운다)
