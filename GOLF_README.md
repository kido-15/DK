# ⛳ 골프장 티타임 검색

**출발 위치 · 이동 희망 시간 · 가격** 세 가지를 넣으면 조건을 만족하는 예약 가능한 티타임을 찾아 줍니다.

```
출발지 주소  ─┐
희망 티오프  ─┼─→  [예약사이트 크롤링] → [골프장 좌표 매칭] → [길찾기 API] → 조건 충족 티타임 목록
그린피 상한  ─┘
```

외부 파이썬 패키지가 필요 없습니다. `pip install` 없이 Python 3.9 이상이면 바로 돌아갑니다.

---

## 🚀 빠른 시작 (3분)

### 1단계 — 화면부터 확인

동봉된 샘플 데이터로 바로 띄워 볼 수 있습니다.

```bash
python3 golf_web.py --courses data/golf/courses.sample.csv \
                    --teetimes data/golf/teetimes.sample.csv
```

브라우저에서 <http://127.0.0.1:8899> 를 엽니다.
출발 위치에 `37.4979,127.0276` (강남역 좌표)을 넣고 검색해 보세요.

> ⚠️ 샘플 데이터의 좌표는 **대략값**이고 티타임은 **가짜**입니다. 화면 확인용일 뿐입니다.

### 2단계 — 진짜 골프장 좌표 받기

```bash
python3 scripts/fetch_golf_courses.py
```

OpenStreetMap에서 국내 골프장의 실제 이름과 좌표를 받아 `data/golf/courses.csv` 를 만듭니다.
키가 필요 없고 한 번만 실행하면 됩니다.

시도 정보가 비어 있는 곳이 많으면:

```bash
python3 scripts/fetch_golf_courses.py --reverse-geocode
```

### 3단계 — 예약 사이트 연결

**로그인이 필요 없는 카카오골프예약부터** 시작하는 편이 수월합니다.
날짜를 눌러야 목록이 바뀌는 사이트라, 날짜 클릭은 자동으로 해 줍니다.

```bash
python3 scripts/auto_collect.py "카카오골프예약 티타임 목록 주소" --show --days 2
```

엑스골프는 로그인이 필요합니다. 먼저 로그인해 두세요.

```bash
python3 scripts/login.py xgolf
```

브라우저 창에서 직접 로그인하시면 됩니다 (아이디·비밀번호는 저장되지 않습니다).
그다음 연결합니다.

```bash
python3 scripts/setup_sites.py xgolf
```

엑스골프 · 카카오골프예약 · 골팡을 순서대로 연결합니다.
브라우저에서 목록 주소를 복사해 붙여 넣기만 하면 됩니다.
자세한 절차는 아래 [예약 사이트 연결하기](#️-예약-사이트-연결하기-엑스골프--카카오골프예약--골팡)를 보세요.

### 4단계 — 골프장 홈페이지 직접 수집 (선택)

플랫폼을 거치지 않고 골프장 공식 홈페이지에서 직접 읽어 옵니다.

```bash
python3 scripts/manage_homepages.py --status
python3 scripts/crawl_all.py --limit 20
python3 golf_web.py --snapshot
```

아래 [골프장 홈페이지 직접 수집](#-골프장-홈페이지-직접-수집)을 보세요.
**상당수 골프장이 로그인을 요구**하므로 플랫폼 수집과 병행하는 편이 좋습니다.

---

## 🖥️ 사용법

### 웹 대시보드

```bash
python3 golf_web.py                       # http://127.0.0.1:8899
python3 golf_web.py --port 9000
python3 golf_web.py --teetimes 내파일.csv   # CSV를 소스로 추가
```

기본값은 `127.0.0.1` 바인딩이라 **내 PC에서만** 열립니다.

### 터미널

```bash
# 강남역 출발, 90분 이내, 20만원 이하, 오전 6~9시 티오프
python3 golf_cli.py --from "서울 강남구 테헤란로 152" \
                    --time 06:00-09:00 --max-drive 90 --max-price 200000

# 좌표로 직접 지정 + 날짜 지정 + 가격순 정렬
python3 golf_cli.py --from 37.4979,127.0276 --date 2026-09-20 \
                    --max-drive 60 --sort price

# 지역 한정, 결과를 CSV로 저장
python3 golf_cli.py --from 37.4979,127.0276 --region 경기,충북 --save 결과.csv
```

| 옵션 | 설명 |
|---|---|
| `--from` | 출발 위치. 주소·역명·건물명 또는 `위도,경도` |
| `--time` | 희망 티오프 시간대. `06:00-09:00` |
| `--max-drive` | 편도 이동 시간 상한 (분) |
| `--max-price` | 1인 그린피 상한 (원) |
| `--date` | 플레이 날짜 |
| `--region` | 지역 필터. 콤마 구분 |
| `--sort` | `score`(추천) / `price` / `drive` / `tee_time` |
| `--routing` | 길찾기 제공자 순서 |
| `--list-sources` | 설정된 소스 목록 |
| `--test-source ID` | 소스 하나를 시험 호출 |

---

## 🕸️ 예약 사이트 연결하기 (엑스골프 · 카카오골프예약 · 골팡)

### 마법사로 한 번에

```bash
python3 scripts/setup_sites.py
```

세 사이트를 순서대로 안내합니다. 브라우저에서 목록 주소를 복사해 붙여 넣으면
구조 분석 → 셀렉터 추출 → 설정 저장 → 시험 호출까지 자동으로 끝납니다.

```bash
python3 scripts/setup_sites.py xgolf      # 한 곳만 다시
python3 scripts/setup_sites.py --status   # 현재 상태 확인
```

> ⚠️ **반드시 국내 PC에서 실행하세요.** 국내 예약 사이트는 해외 IP를 차단하는 경우가 많습니다.

### 왜 셀렉터가 미리 안 들어 있나요

엑스골프·카카오골프예약·골팡 **모두 공개 API를 제공하지 않습니다.**
화면을 직접 읽어 오는 수밖에 없는데, 구조가 사이트마다 다르고 개편 때마다 바뀝니다.
지금 맞는 셀렉터를 적어 두어도 다음 개편에 깨지기 때문에, 추측값을 넣는 대신
**실제 응답에서 찾아내는 도구**를 넣었습니다.

### 목록 주소 찾는 법 — 주소창 복사면 끝

**개발자도구(F12)를 열 필요 없습니다.**

1. 브라우저에서 해당 사이트의 **티타임 목록 화면**을 엽니다
2. **주소창의 주소를 복사**합니다
3. 마법사에 붙여 넣습니다

로그인해야 보이는 화면이라면 [먼저 로그인](#-로그인이-필요한-사이트-엑스골프--카카오골프예약)해 두면 됩니다.

주소에 박힌 날짜(`20260920`)와 페이지 번호는 **마법사가 치환자로 자동 변환**합니다.
그래야 매번 원하는 날짜로 조회됩니다.

### 자바스크립트로 그려지는 화면도 됩니다

플랫폼 예약 사이트는 대부분 목록을 자바스크립트로 그립니다. 주소만 받아 오면
빈 껍데기라 아무것도 없습니다. 그래서 마법사가 **일반 요청으로 실패하면 자동으로
브라우저를 띄웁니다.**

```
  일반 요청으로는 목록을 찾지 못했습니다.
  브라우저를 띄워 화면을 직접 열고, 오가는 데이터를 살펴보겠습니다.

  브라우저를 띄워 볼까요? (20~30초 걸립니다) (Y/n): y
  여는 중...
  ✓ 티타임 24건을 찾았습니다
  ✓ 목록 API 를 찾았습니다
      https://사이트/api/v2/teetimes?playDate=20260920&page=1
      레코드 위치: data.teeList
    이 주소를 저장하면 다음부터는 브라우저 없이 빠르게 수집합니다.
```

브라우저가 페이지를 열면서 **오가는 JSON을 가로채 목록 API를 스스로 찾아냅니다.**
개발자도구에서 XHR을 뒤지는 일을 대신하는 것입니다.

찾아낸 API는 필드 매핑까지 설정에 저장되므로, **이후에는 브라우저 없이 몇 초 만에**
같은 목록을 받아 옵니다. API를 못 찾으면 매번 브라우저로 여는 설정으로 남습니다(느리지만 동작).

브라우저 모드에는 Playwright가 필요합니다.

```bash
pip3 install playwright
python3 -m playwright install chromium
```

### 🔐 로그인이 필요한 사이트 (엑스골프)

엑스골프는 **로그인해야 티타임이 보입니다.** 먼저 로그인해 두어야 합니다.
(카카오골프예약은 로그인 없이도 목록이 보입니다.)

```bash
python3 scripts/login.py xgolf
```

브라우저 창이 열리면 **직접 로그인**하시고, 터미널로 돌아와 Enter를 누르면 됩니다.
그 로그인 상태가 저장되어 이후 수집에 쓰입니다.

```bash
python3 scripts/login.py            # 세 곳을 순서대로
python3 scripts/login.py --status   # 어디에 로그인돼 있는지
python3 scripts/login.py --clear xgolf
```

로그인한 뒤 연결하면, 마법사가 알아서 그 로그인 상태를 쓰도록 설정합니다.

```bash
python3 scripts/login.py xgolf        # 1. 로그인
python3 scripts/setup_sites.py xgolf  # 2. 연결
```

#### 무엇이 저장되나요

| 저장됨 | 저장 안 됨 |
|---|---|
| 브라우저 쿠키, 브라우저 프로필 | **아이디, 비밀번호** |

아이디와 비밀번호는 이 프로그램이 **보지도, 저장하지도 않습니다.** 직접 입력하신 내용은
브라우저와 해당 사이트 사이에서만 오갑니다. 이 프로그램은 로그인을 대신하지 않고,
**kd님이 직접 로그인한 결과를 이어서 쓸 뿐**입니다.

#### ⚠️ 로그인 기능을 쓸 때 주의할 점

- 저장 폴더(`data/golf/sessions/`)에는 **로그인된 상태**가 들어 있습니다.
  가져간 사람이 그대로 로그인되므로 **남과 공유하지 마세요.** (git에서도 제외돼 있습니다)
- 세션은 시간이 지나면 만료됩니다. 수집이 안 되면 다시 로그인하세요.
- **본인 계정으로, 본인이 볼 수 있는 화면만** 읽습니다.
- 각 사이트 이용약관에서 자동 수집을 금지한다면 사용하지 마세요.
  **과도하게 요청하면 계정이 제한될 수 있습니다.** 요청 간격을 넉넉히 두세요.

#### 로그인 후에도 안 될 때

```bash
python3 scripts/login.py --status        # 세션이 살아 있는지
python3 scripts/login.py --clear xgolf   # 지우고
python3 scripts/login.py xgolf           # 다시 로그인
```

### 🌐 쓸 브라우저 고르기

기본은 Playwright가 내려받은 크로미움입니다. 평소 쓰는 브라우저를 지정할 수도 있습니다.

```bash
python3 scripts/login.py --list-browsers
```

```
  이름                           제어 가능      경로
  ----------------------------------------------------------------
  Aside                          가능         /Applications/Aside.app/...
  Google Chrome                  가능         /Applications/Google Chrome.app/...
  Safari                         어려움        /Applications/Safari.app/...
```

http·https 를 여는 앱으로 등록된 것만 목록에 올립니다. Electron으로 만든 앱
(메신저·편집기)이나 오피스 앱은 크로미움을 품고 있어도 브라우저가 아니므로 빠집니다.

이름으로 지정합니다.

```bash
python3 scripts/login.py xgolf --browser Aside
python3 scripts/setup_sites.py xgolf --browser Chrome
```

> Playwright는 **크로미움 계열·파이어폭스·웹킷**만 제어할 수 있습니다.
> 크로미움 기반 브라우저(Chrome, Edge, Brave, Arc, Aside, Whale 등)는 대부분 됩니다.
> 목록에 "어려움"으로 나와도 크로미움 기반이면 동작할 수 있으니 시도해 보세요.
> 경로를 직접 줘도 됩니다: `--browser "/Applications/이름.app"`

### 🔗 이미 열어 둔 브라우저에 붙기

**가장 편한 방법입니다.** 평소 쓰는 브라우저를 원격 디버깅을 켠 채로 띄워 두면,
새 창을 만들지 않고 **그 브라우저에 붙습니다.** 이미 로그인해 둔 상태를 그대로 쓰므로
따로 로그인할 필요가 없습니다.

```bash
python3 scripts/login.py --help-connect
```

절차는 이렇습니다.

1. 브라우저를 완전히 종료합니다 (창만 닫지 말고 앱을 끄세요)
2. 원격 디버깅을 켜고 띄웁니다

   ```bash
   open -a "Aside" --args --remote-debugging-port=9222
   ```

   (앱 이름만 바꾸면 다른 브라우저도 같습니다)

3. 그 브라우저에서 예약 사이트에 로그인해 둡니다
4. 붙어서 수집합니다

   ```bash
   python3 scripts/setup_sites.py xgolf --connect http://localhost:9222
   ```

환경변수로 지정해 두면 매번 붙습니다.

```bash
export GOLF_BROWSER_CDP=http://localhost:9222
```

| 이 방식의 좋은 점 | 주의할 점 |
|---|---|
| 이미 로그인된 상태를 그대로 사용 | 원격 디버깅 포트가 열린 동안에는 **같은 컴퓨터의 다른 프로그램도** 그 브라우저를 제어할 수 있습니다. 수집이 끝나면 껐다가 평소대로 다시 여세요 |
| 캡차·2단계 인증을 직접 처리 가능 | 브라우저를 한 번 완전히 종료해야 합니다 |
| 세션을 따로 저장할 필요 없음 | |

> 붙은 브라우저는 **닫지 않습니다.** 새 탭만 열고, 다 읽으면 그 탭만 닫습니다.

### 🖐 드롭다운으로 골라야 목록이 나오는 사이트

엑스골프처럼 **날짜·지역·시간을 드롭다운에서 고르고 "검색"을 눌러야** 티타임이
나오는 사이트가 많습니다. 이런 화면은 주소만으로는 재현되지 않습니다.
주소를 그대로 넣으면 조건 입력 폼만 받아 와 티타임이 0건입니다.

그래서 **보고 있는 화면을 그대로 읽는** 방법을 씁니다.

```bash
open -a "Aside" --args --remote-debugging-port=9222
```

브라우저에서 **직접 로그인하고, 조건을 고르고, 검색을 눌러** 티타임이 줄줄이
보이는 화면을 만드세요. 그 상태로 두고 실행합니다.

```bash
python3 scripts/grab.py
```

```
열려 있는 탭 2개

  [1] 엑스골프 - 실시간예약
      https://www.xgolf.com/booking/...
  [2] 새 탭
      about:blank

티타임이 보이는 탭의 번호를 입력하세요. (그냥 Enter 면 1번)
  번호: 1

티타임 24건

  남서울컨트리클럽          2026-09-20 11:12    168,000원
  레이크사이드CC          2026-09-20 12:40    132,000원
  ...
```

**여러 날짜를 한 번에** 모으려면 `--days` 를 붙입니다. 화면은 그대로 두고
날짜만 프로그램이 눌러 가며 모읍니다.

```bash
python3 scripts/grab.py --days 7
```

```
  2026-09-20     3건  data-date="2026-09-20" → tr.item 구조에서 3건 추출
  2026-09-21     2건  data-date="2026-09-21" → tr.item 구조에서 2건 추출
  2026-09-22     1건  data-date="2026-09-22" → 이미 확인된 구조로 1건 추출
```

검색에 바로 쓰려면 `--add-snapshot` 을 붙입니다.

```bash
python3 scripts/grab.py --add-snapshot
```
```bash
python3 golf_cli.py --snapshot --from 37.4979,127.0276 --time 11:00-15:00 --max-price 150000
```

| 옵션 | 설명 |
|---|---|
| `--list` | 열린 탭 목록만 보기 |
| `--tab 2` | 2번 탭을 바로 읽기 |
| `--days 7` | 그 탭에서 날짜를 눌러 가며 며칠치 수집 |
| `--date 2026-10-15` | 화면에 날짜가 없을 때 지정 |
| `--save 결과.csv` | CSV로 저장 |
| `--add-snapshot` | 검색에 쓰이도록 저장 |

> 페이지를 새로 열거나 이동시키지 않습니다. **보고 있는 화면을 그대로** 읽습니다.
> 드롭다운에 들어 있는 다른 시각(고르지 않은 06:00, 15:00 등)은 결과에 섞이지 않습니다.

**이 방법이 가장 확실합니다.** 로그인·캡차·복잡한 검색 조건을 kd님이 직접 처리하고,
프로그램은 결과 화면만 읽으면 되기 때문입니다.

### 🔎 안 될 때 — 사이트 구조 조사하기

무엇이 왜 안 되는지 알아내는 도구입니다. 페이지를 열어 **오간 JSON 요청, 목록 구조,
날짜 고르는 부분**을 전부 기록하고, 날짜를 실제로 눌러 본 결과까지 남깁니다.

```bash
python3 scripts/explore.py "티타임 목록 주소" --show
```

이미 열어 둔 브라우저의 탭도 조사할 수 있습니다.

```bash
python3 scripts/explore.py --connect http://localhost:9222 --tab 1
```

보고서는 이런 식으로 나옵니다.

```
  ① 페이지가 부른 JSON 요청
  [1] GET https://사이트/api/v2/teetimes?playDate=20260920&page=1
      배열 'data.teeList' — 4건
        첫 레코드: {'ccName': '가나CC', 'teeTime': '0630', 'greenFee': '168000'}
        추측한 필드: {'course_name': 'ccName', 'tee_time': 'teeTime', ...}
      ★ 이 응답에서 티타임 4건을 뽑을 수 있습니다

  ③ 날짜를 고르는 부분
  data-date 속성을 가진 요소 4개
    <A> data-date="2026-09-20" 글자="20 일"

  ④ 날짜를 실제로 눌러 본 결과
  2026-09-20  성공 → 티타임 3건
```

화면 내용을 그대로 복사해 보여 주시면 **맞춤 설정을 만들어 드릴 수 있습니다.**
스크린샷과 HTML도 `data/golf/` 에 함께 저장됩니다.

> 로그인한 상태로 조사하면 이름이나 예약 내역이 보고서에 섞일 수 있습니다.
> 남에게 보낼 때 한 번 훑어보세요.

### 설정한 뒤 확인

```bash
python3 golf_cli.py --list-sources
python3 golf_cli.py --test-source xgolf
python3 golf_cli.py --test-source golfpang --date 2026-10-15
```

### 크롤링할 때 지킬 것

- `delay_seconds` 를 **1초 이상** 두세요 (기본 1.5초). 짧으면 사이트에 부담을 주고 차단당합니다
- `respect_robots` 는 `true` 로 두세요. robots.txt가 막은 주소는 자동으로 건너뜁니다
- 로그인이 필요한 사이트는 **직접 로그인해 두고** 그 세션을 쓰세요 (위 참고).
  아이디·비밀번호를 프로그램에 넣어 자동 로그인하는 방식은 지원하지 않습니다
- 각 사이트 이용약관에서 자동 수집을 금지하는지 먼저 확인하세요. 금지된 사이트라면 연결하지 마세요

### 크롤링이 막히는 사이트라면

CSV로 직접 넣어도 똑같이 검색됩니다.

```csv
course_name,play_date,tee_time,green_fee,booking_url
남서울CC,2026-09-20,06:48,175000,https://...
```

```bash
python3 golf_cli.py --from 37.4979,127.0276 --teetimes 내파일.csv
```

---

## 🎯 시간대와 가격으로 좁히기

### 시간대는 범위로 지정합니다

정해진 한 시각이 아니라 **구간 안에 있는 티타임**을 찾습니다.

```bash
python3 golf_cli.py --from 37.4979,127.0276 --time 11:00-15:00
```

웹에서는 시간 두 칸을 직접 넣거나 빠른 선택 버튼을 쓰면 됩니다.

| 버튼 | 범위 |
|---|---|
| 새벽 | 05:00 ~ 08:00 |
| 오전 | 08:00 ~ 11:00 |
| 낮 | 11:00 ~ 15:00 |
| 오후 | 15:00 ~ 19:00 |
| 전체 | 제한 없음 |

### 가격은 상한입니다

**기재한 금액 이하**만 나옵니다.

```bash
python3 golf_cli.py --from 37.4979,127.0276 --max-price 150000
```

가격이 안 적힌 티타임은 상한을 확인할 수 없어 **기본적으로 제외**됩니다.
포함하려면:

```bash
python3 golf_cli.py --from ... --max-price 150000 --include-unknown-price
```

웹에서는 그린피 칸 아래 "가격 미기재도 포함" 체크박스입니다.

### 합쳐서 쓰기

```bash
python3 golf_cli.py --from "서울 강남구 테헤란로 152" \
                    --time 11:00-15:00 --max-price 150000 --max-drive 90 --sort price
```

> 같은 티타임이 여러 번 수집되면 **골프장·시각·가격·예약처가 모두 같은 것**은
> 하나로 합쳐집니다. 몇 건이 합쳐졌는지는 진단 정보에서 볼 수 있습니다.

---

## 🗓 날짜를 눌러야 하는 사이트 자동 수집 (카카오골프예약)

카카오골프예약처럼 **날짜를 클릭해야 목록이 바뀌는** 사이트는 주소만으로 날짜를
바꿀 수 없습니다. 날짜 클릭을 자동으로 해 줍니다.

```bash
python3 scripts/auto_collect.py "티타임목록주소" --days 7
```

브라우저가 열려서 날짜를 하나씩 눌러 가며 모아 옵니다.

```
  2026-09-20     3건  data-date="2026-09-20" → tr.item 구조에서 3건 추출
  2026-09-21     2건  data-date="2026-09-21" → tr.item 구조에서 2건 추출
  2026-09-22     1건  data-date="2026-09-22" → 이미 확인된 구조로 1건 추출

============================================================
  모두 6건
============================================================
```

**처음에는 브라우저를 보면서 확인하세요.**

```bash
python3 scripts/auto_collect.py "주소" --show --days 2
```

| 옵션 | 설명 |
|---|---|
| `--days 7` | 오늘부터 며칠치 |
| `--dates 2026-09-20,2026-09-21` | 날짜를 직접 지정 |
| `--show` | 브라우저를 보면서 진행 |
| `--search 검색` | 날짜를 고른 뒤 눌러야 하는 버튼 |
| `--connect http://localhost:9222` | 이미 열어 둔 브라우저에 붙기 |
| `--browser Aside` | 쓸 브라우저 지정 |
| `--save 결과.csv` | CSV로도 저장 |

수집이 끝나면 바로 검색할 수 있습니다.

```bash
python3 golf_cli.py --snapshot --from 37.4979,127.0276 --time 11:00-15:00 --max-price 150000
```

### 날짜를 어떻게 찾나요

사이트마다 날짜 부분이 다르게 생겼습니다. 아래 순서로 찾아 눌러 봅니다.

1. `data-date="2026-09-20"` 처럼 **속성에 날짜가 박힌** 요소
2. `9/20`, `9월 20일`, `09.20` 처럼 **날짜가 적힌** 누를 수 있는 항목
3. **달력 영역 안**의 날짜 숫자
4. **드롭다운**에 날짜가 있는 경우

> **누른 뒤 목록이 실제로 바뀌었는지 확인합니다.** 바뀌지 않으면 그 날짜는
> 건너뜁니다. 확인하지 않으면 같은 목록을 여러 날짜 것으로 저장하게 됩니다.

같은 글자가 감싸는 `<li>` 와 실제 클릭 핸들러가 달린 `<a>` 양쪽에 걸리는 경우가
많아, 누를 만한 순서대로 몇 개를 시도합니다.

### 잘 안 될 때

```bash
python3 scripts/auto_collect.py "주소" --show --days 2
```

브라우저를 띄워 무엇을 누르는지 직접 보세요. 날짜를 못 찾으면 화면의 날짜 관련
요소를 알려 줍니다.

그래도 안 되면 **직접 화면을 만들어 읽히는** 방법이 있습니다.

```bash
python3 scripts/grab.py
```

---

## 🏌️ 골프장 홈페이지 직접 수집

플랫폼(엑스골프 등)을 거치지 않고 **골프장 공식 홈페이지에서 직접** 티타임을 읽어 옵니다.
골프장마다 셀렉터를 적어 줄 수 없으므로, 페이지를 보고 **스스로 판단**합니다.

```
홈페이지 → 예약 링크 찾기 → 예약 화면 열기 → 티타임 자동 추출 → 성공 경로 기억
```

### 실행

```bash
python3 scripts/manage_homepages.py --status     # 1. 홈페이지 주소가 얼마나 채워졌나
python3 scripts/crawl_all.py --limit 20          # 2. 20곳만 시험 수집
python3 scripts/crawl_all.py --days 7            # 3. 전체를 7일치 수집
python3 golf_web.py --snapshot                   # 4. 수집 결과로 검색
```

수백 곳을 도는 작업이라 시간이 걸립니다. **처음에는 반드시 `--limit 20` 으로 시험**하세요.

| 명령 | 설명 |
|---|---|
| `crawl_all.py --report` | 수집하지 않고 현황만 (어디가 되고 어디가 막히는지) |
| `crawl_all.py --region 경기,충북` | 지역 한정 |
| `crawl_all.py --date 2026-10-15` | 특정 날짜만 |
| `crawl_all.py --retry-failed` | 실패했던 곳도 다시 |
| `crawl_all.py --workers 6` | 동시 처리 수 (기본 6, 너무 높이지 마세요) |

### ⚠️ 한계 — 미리 아셔야 합니다

**개별 골프장 홈페이지는 상당수가 회원 로그인 후에만 티타임을 보여 줍니다.**
이 프로그램은 로그인 벽을 만나면 기록만 남기고 넘어갑니다. 로그인 세션을 흉내 내는 것은
약관 위반 소지가 크기 때문에 하지 않습니다.

수집이 안 되는 대표적인 경우:

| 상태 | 뜻 | 대응 |
|---|---|---|
| **로그인 필요** | 회원만 티타임 조회 가능 | 플랫폼(`setup_sites.py`)으로 보세요 |
| **자바스크립트 화면** | 목록이 브라우저에서 그려짐 | 예약 API 주소를 찾아 `homepage` 에 직접 넣으면 됩니다 |
| **예약 링크 못 찾음** | 홈페이지에서 예약 메뉴를 못 찾음 | 예약 페이지 주소를 `homepage` 에 직접 넣으세요 |
| **홈페이지 주소 없음** | `courses.csv` 의 `homepage` 가 빔 | 아래 참고 |

그래서 **플랫폼 수집과 병행하는 것이 가장 실용적입니다.**

```bash
python3 golf_web.py --with-snapshot    # 플랫폼 + 홈페이지 수집 결과를 함께
```

### 홈페이지 주소 채우기

수집의 출발점입니다. OpenStreetMap에 `website` 태그가 있는 곳은 자동으로 채워지지만,
없는 곳이 많습니다.

```bash
python3 scripts/manage_homepages.py --status               # 얼마나 비었나
python3 scripts/manage_homepages.py --export 채울목록.csv    # 엑셀로 내보내기
# 엑셀에서 homepage 칸을 채운 뒤
python3 scripts/manage_homepages.py --import 채울목록.csv
```

한 곳씩 넣거나, 자동으로 찾을 수도 있습니다.

```bash
python3 scripts/manage_homepages.py --set "남서울CC=https://www.namseoulcc.co.kr"
python3 scripts/manage_homepages.py --search      # 네이버 검색 API (키 필요)
python3 scripts/manage_homepages.py --check       # 주소가 살아 있는지 확인
```

> 자동 검색은 틀릴 수 있습니다. `--check` 로 확인하고 `--clear-dead` 로 죽은 주소를 정리하세요.

### 같은 예약 솔루션 묶어 보기

골프장들은 자체 개발 대신 소수의 예약 솔루션을 공유해 씁니다.
**한 곳이 풀리면 같은 솔루션을 쓰는 나머지도 같이 풀립니다.**

```bash
python3 scripts/crawl_all.py --report
```

```
같은 예약 솔루션을 쓰는 골프장 묶음
  script:booking.vendor.co.kr                    37곳
      가나CC, 다라CC, 마바CC ...
```

어떤 솔루션이 많이 쓰이는지는 미리 정해 두지 않고 **수집하면서 자동으로 파악**됩니다.

### 특가 변동 추적

수집할 때마다 결과가 `data/golf/snapshots/` 에 날짜별로 쌓이고, 직전 수집과 비교됩니다.

```
이전 수집(2026-09-16.json) 대비 변동
■ 가격이 내려간 티타임 3건
   가나CC 2026-09-20 06:30  180,000원 → 150,000원 (-30,000원)
■ 새로 올라온 티타임 12건
```

매일 자동으로 돌리려면 `crontab -e` 에 추가하세요.

```
0 7,12,19 * * * cd ~/golf-DK && /usr/bin/python3 scripts/crawl_all.py --days 7 >> crawl.log 2>&1
```

### 수집할 때 지키는 것

- 사이트당 요청 간격 **1.5초**(기본), 동시 처리 6곳
- `robots.txt` 준수
- 로그인이 필요한 페이지는 건드리지 않음
- 한 번 성공한 경로를 기억해 **불필요한 재탐색을 하지 않음**
- 로그인 벽으로 확인된 곳은 다시 두드리지 않음

---

## 🚗 이동 시간 계산

제공자를 순서대로 시도하고, 실패하면 다음으로 넘어갑니다. 마지막 `estimate`는 네트워크 없이 항상 성공하므로 **검색이 멈추는 일은 없습니다.**

| 제공자 | 키 | 정확도 |
|---|---|---|
| `kakao` | 필요 | 국내 도로 기준 가장 정확 |
| `ors` | 무료 키 | 실제 도로 기준 |
| `osrm` | 불필요 | 실제 도로 기준, 공개 서버라 느릴 수 있음 |
| `estimate` | 불필요 | 직선거리 × 1.3 ÷ 구간별 평균속도 — **추정값** |

```bash
export KAKAO_REST_API_KEY="발급받은키"     # 카카오 개발자 사이트에서 발급
export ORS_API_KEY="발급받은키"            # openrouteservice.org
python3 golf_cli.py --routing kakao,osrm,estimate --from ...
```

**어떤 방식으로 계산했는지는 결과에 항상 표시됩니다.** 웹에서는 이동 시간 옆 작은 글씨, 터미널에서는 맨 아래 요약에 나옵니다. `추정`으로 표시된 값은 실제 도로 소요시간과 다를 수 있습니다.

> 카카오 키가 없어도 동작하지만, `estimate`는 특히 산간 지역 골프장에서 오차가 큽니다. 자주 쓸 거라면 카카오 키 발급을 권합니다.

---

## 🔗 골프장 이름 매칭

예약 사이트는 `남서울CC`, 마스터 DB는 `남서울컨트리클럽` — 표기가 다릅니다.
`컨트리클럽/CC/골프클럽` 같은 군더더기를 떼고 비교하며, 완전 일치 → 포함 관계 → 유사도 순으로 찾습니다.

그래도 못 찾으면 **이동 시간을 계산할 수 없어 결과에서 빠집니다.** 웹의 "진단 정보 보기", 터미널의 마지막 줄에 실패한 이름이 나옵니다.

해결: `data/golf/courses.csv` 에서 해당 골프장 행의 `aliases` 칸에 그 이름을 추가합니다 (여러 개는 `|` 로 구분).

```csv
course_id,name,lat,lon,region,address,holes,phone,source,aliases
osm-way-123,남서울컨트리클럽,37.38,127.09,경기,...,18,,osm,남서울CC|남서울골프
```

`fetch_golf_courses.py --merge` 로 다시 받으면 직접 넣은 별칭이 보존됩니다.

---

## 📁 구성

```
golf_web.py                     웹 대시보드 실행
golf_cli.py                     터미널 검색

golf/
  models.py                     Course, TeeTime, SearchQuery + 가격·시간 파싱
  interact.py                   날짜 클릭·더보기 등 화면 조작
  extract.py                    설정 없이 티타임을 뽑아내는 자동 추출 엔진
  profiles.py                   골프장별 크롤링 프로필 학습, 예약 솔루션 묶기
  snapshot.py                   일별 수집 결과 저장과 특가 변동 비교
  geo.py                        거리 계산, 지오코딩
  routing.py                    이동 시간 (kakao/ors/osrm/estimate)
  courses.py                    골프장 DB, 이름 매칭
  search.py                     필터 → 거리 프리필터 → 길찾기 → 점수
  htmlsel.py                    의존성 없는 HTML 파서 + CSS 선택자
  server.py                     대시보드 서버
  static/                       화면 (HTML/CSS/JS)
  sources/
    web_source.py               설정 기반 범용 크롤러 (플랫폼용)
    site_crawler.py             골프장 홈페이지 직접 수집
    browser_source.py           브라우저로 열고 목록 API 가로채기
    snapshot_source.py          저장된 수집 결과 읽기
    csv_source.py               CSV 소스

scripts/
  explore.py                    사이트 구조 조사 보고서 (안 될 때 원인 찾기)
  auto_collect.py               날짜를 눌러 가며 자동 수집 (카카오골프예약 등)
  grab.py                       지금 보고 있는 브라우저 화면을 그대로 수집
  login.py                      예약 사이트 로그인 (직접 로그인한 세션을 저장)
  setup_sites.py                예약 사이트 연결 마법사 (엑스골프/카카오/골팡)
  crawl_all.py                  전체 골프장 홈페이지 병렬 수집
  manage_homepages.py           골프장 홈페이지 주소 채우기
  fetch_golf_courses.py         OSM에서 골프장 좌표 수집
  probe_source.py               사이트 구조 분석 → 설정 초안 생성

config/sources.example.json     소스 설정 서식
data/golf/*.sample.csv          데모용 샘플 (가짜 데이터)
data/golf/sessions/             로그인 세션 (git 제외, 공유 금지)
tests/fake_site.py              플랫폼 크롤러 검증용 가짜 사이트
tests/fake_courses.py           골프장 홈페이지 검증용 가짜 사이트 6곳
tests/fake_spa.py               자바스크립트 렌더링 검증용 가짜 플랫폼
tests/fake_login_site.py        로그인 필요 사이트 검증용 가짜 플랫폼
tests/fake_dropdown_site.py     드롭다운 검색형 사이트 검증용
tests/fake_datepick_site.py     날짜 클릭형 사이트 검증용
```

### 검색이 도는 순서

1. 소스에서 티타임 수집
2. 골프장 이름 → 좌표 매칭
3. 날짜·시간·가격으로 거르기 *(싼 연산 먼저)*
4. **직선거리로 한 번 더 거르기** — 길찾기 API 호출을 줄이는 핵심 단계
5. 남은 것만 실제 이동 시간 조회
6. 점수 매겨 정렬 *(가격 40% + 이동시간 40% + 희망시간 근접도 20%)*

4단계가 없으면 골프장 500곳 × 티타임 수만큼 API를 호출하게 됩니다. 이동 시간 상한을 걸면 갈 수 없는 거리를 미리 잘라내 호출 수가 크게 줄어듭니다. 실제로 얼마나 호출됐는지는 진단 정보에서 확인할 수 있습니다.

---

## ⚠️ 알아 두실 것

- **그린피와 잔여 좌석은 원 사이트 값을 그대로 옮긴 것입니다.** 카트비·캐디피 포함 여부가 사이트마다 다르고, 크롤링 시점과 실제 예약 시점 사이에 바뀔 수 있습니다. **결제 전 반드시 원 사이트에서 확인하세요.**
- 이 프로그램은 **검색만 합니다.** 예약은 링크를 눌러 해당 사이트에서 직접 하셔야 합니다.
- `estimate`로 계산된 이동 시간은 추정값입니다. 실제 교통 상황은 반영되지 않습니다.
- 샘플 데이터(`*.sample.csv`)의 좌표는 대략값, 티타임은 가짜입니다.
