# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/

# 개인 설정 — 각자 사이트를 직접 설정해야 하므로 공유하지 않는다
config/sources.json

# 생성물 — scripts/fetch_golf_courses.py 로 각자 받는다
data/golf/courses.csv
data/golf/teetimes.csv

# 수집 결과와 학습된 프로필 — 각자 실행하며 쌓인다
data/golf/snapshots/
data/golf/site_profiles.json
data/golf/probe-*.txt
crawl.log

# 로그인 세션 — 절대 커밋하지 말 것.
# 이 폴더에는 로그인된 상태(쿠키)가 들어 있어, 가져간 사람이 그대로 로그인됩니다.
data/golf/sessions/
